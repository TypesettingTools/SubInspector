[wrap-git]
directory = harfbuzz
# The old boxerab/harfbuzz "meson" branch fork was deleted upstream;
# harfbuzz ships its own meson.build since 2.6.5, so track a stable tag.
url = https://github.com/harfbuzz/harfbuzz.git
revision = 2.6.5
clone-depth = 1
